. /usr/lib/ybs/funcs

post_install() {
	gnome2_desktop_database_update
	# Version that less than 20120328 MUST Restart daemon
	#version_old=$3
	#ypkg --compare-version $version_old 20120328
	#
	#if [ $? -eq 2 ]; then
	echo "* Wait and restart daemon.."
	/usr/bin/ycenter_daemon_restart.sh &
	#fi
	return 0
}

post_upgrade() {
	post_install
	
}

post_downgrade() {
	post_install
}

