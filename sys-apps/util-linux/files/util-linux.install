. /usr/lib/ybs/funcs

post_install() {
	if [ ! -d /run/uuidd ]; then
    		# refer to uid/gid by number to avoid dependency on filesystem
		install -o 68 -g 68 -dm755 /run/uuidd
	fi
	return 0
}

post_upgrade() {
	post_install
}

