. /usr/lib/ybs/funcs

pre_install() {
    dogroupadd -r "apache"
    douseradd -c "Apache Server" -d "/dev/null" -s "/bin/false" -r -N "apache"
    return 0
}

post_install() {
    chown -R apache:apache /srv/www
}

post_remove() {
    douserdel  "apache"
    dogroupdel "apache"
    return 0
}
