. /usr/lib/ybs/funcs

post_install() {
    depmod -a "$KERNEL_DIST"
    systemd-tmpfiles --create lirc.conf ||true
}

post_remove() {
    depmod -a "$KERNEL_DIST" || true
}

post_upgrade() {
    post_install
}
