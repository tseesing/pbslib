
conf="/etc/ybs.conf"
comf_sample="/usr/share/ybs/ybs.conf.sample"

post_install() {
	[ ! -e "$conf" ] && cp "$comf_sample" "$conf" 
	return 0
}

post_upgrade() {
	post_install
}

post_remove() {
	rm -r "$conf" ||true
}

