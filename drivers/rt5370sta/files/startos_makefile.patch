diff -ur rt5370sta-2.5.0.2/os/linux/Makefile.6 rt5370sta-2.5.0.2.new//os/linux/Makefile.6
--- rt5370sta-2.5.0.2/os/linux/Makefile.6	2011-04-07 09:46:49.000000000 +0800
+++ rt5370sta-2.5.0.2.new//os/linux/Makefile.6	2011-11-03 15:00:11.711284336 +0800
@@ -356,12 +356,11 @@
 endif
 
 install:
-	rm -rf $(DAT_PATH)
-	$(shell [ ! -f /etc/Wireless ] && mkdir /etc/Wireless)
-	mkdir $(DAT_PATH)
-	cp $(RT28xx_DIR)/$(DAT_FILE_NAME) $(DAT_PATH)/.
-	install -d $(LINUX_SRC_MODULE)
-	install -m 644 -c $(addsuffix .ko,$(MOD_NAME)) $(LINUX_SRC_MODULE)
+	mkdir -p $(DESTDIR)/etc/Wireless
+	mkdir -p $(DESTDIR)/$(DAT_PATH)
+	cp $(RT28xx_DIR)/$(DAT_FILE_NAME) $(DESTDIR)/$(DAT_PATH)/.
+	install -d $(DESTDIR)/$(LINUX_SRC_MODULE)
+	install -m 644 -c $(addsuffix .ko,$(MOD_NAME)) $(DESTDIR)/$(LINUX_SRC_MODULE)
 	/sbin/depmod -a ${shell uname -r}
 
 uninstall:
