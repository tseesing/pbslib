. /usr/lib/ybs/funcs

post_install() {	
	# Update initramfs
	depmod -a "$KERNEL_DIST"
	update-initramfs -d -c -k "$KERNEL_DIST" ||return 0
}       

post_remove() {
	post_install
}

post_upgrade() {
	post_install
}

post_upgrade() {
	post_install
}

