--- grub-1.98/util/hostdisk.c	2011-03-10 17:09:40.960627002 +0800
+++ grub-1.98/util/hostdisk.modified.c	2011-03-10 17:08:51.146627002 +0800
@@ -154,8 +154,10 @@
   unsigned i;
 
   for (i = 0; i < sizeof (map) / sizeof (map[0]); i++)
-    if (map[i].drive && hook (map[i].drive))
+  {
+    if (map[i].drive  && strncmp(map[i].drive, "fd", 2) && hook (map[i].drive))
       return 1;
+  }
 
   return 0;
 }
