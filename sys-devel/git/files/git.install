. /usr/lib/ybs/funcs

gname="git"

post_install() {
    dogroupadd --system $gname
    douseradd --system -c 'git daemon user' -g $gname -d / -s /bin/bash $gname || true  
}

post_remove() {
    douserdel $gname
      dogroupdel $gname || true
}

post_upgrade() {
    post_install
}

