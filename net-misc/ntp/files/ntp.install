. /usr/lib/ybs/funcs

gname="ntp"

post_install() {
    dogroupadd -g 87 $gname
    douseradd -u 87 -g $gname -d /var/lib/ntp -c 'Network Time Protocol' -s /bin/false $gname ||true
}

post_remove() {
    douserdel $gname
    dogroupdel $gname ||true
}

