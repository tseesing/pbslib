. /usr/lib/ybs/funcs

gname="polkitd"

post_install() {
	dogroupadd -g 102 $gname
	douseradd -c 'Policy Kit Daemon' -u 102 -g $gname -d '/' -s /bin/false $gname
	passwd -l $gname &>/dev/null ||true
}

post_remove() {
	douserdel $gname
	dogroupdel $gname ||true
}

post_upgrade() {
	post_install
}

