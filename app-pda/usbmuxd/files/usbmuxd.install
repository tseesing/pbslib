
gname="usbmux"

post_install() {
    dogroupadd -g 140 $gname 
    douseradd -c 'usbmux user' -u 140 -g $gname -d / -s /sbin/nologin $gname 
    passwd -l $gname ||true
}

post_remove() {
    douserdel -f $gname
    dogroupdel $gname || true
}

post_upgrade() {
    post_install
}
