. /usr/lib/ybs/funcs

post_install() {
	lists="make patch automake autoconf patch intltool pkgconfig gcc-dev glib2-dev
		   linux-headers-dev linux-kernel-headers"
	for p in $lists;do 
		yget install -y "${p}"
	done		
	return 0
}
